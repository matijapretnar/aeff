(executable
 (name aeff)
 (libraries ocaml-vdom)
 (modes js)
 (link_flags -no-check-prims)
 (promote
  (until-clean)
  (into ../web)
  (only aeff.bc.js)))

(ocamllex lexer)

(menhir
 (modules parser))
